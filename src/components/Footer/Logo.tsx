import React, { memo, ReactElement } from "react";

const Logo = (): ReactElement => <img src="/images/logo/logo-white.svg" width="180px" height="168.26px" alt="Artalgie"/>

export default memo(Logo);
